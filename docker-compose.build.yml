# Local development override: builds from source instead of pulling the
# published image.
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
services:
  autostart:
    build:
      context: .
      args:
        VERSION: dev
    image: docker-autostart:dev
